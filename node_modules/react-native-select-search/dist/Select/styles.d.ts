import { ViewStyle, TextStyle } from "react-native";
interface IStyles {
    container: ViewStyle;
    dropDownContainer: ViewStyle;
    noResultsText: TextStyle;
}
export declare const styles: IStyles;
export {};
