import { StyleSheet } from "react-native";
import { Colors } from "../../../Utils/index";
export const styles = (containerBackgroundColor) => StyleSheet.create({
    container: {
        backgroundColor: containerBackgroundColor
            ? containerBackgroundColor
            : Colors.alabaster,
        flex: 1,
        flexDirection: "row",
        width: "100%",
    },
    text: {
        fontSize: 17,
    },
});
